class Solution:
    def findPrefixScore(self, nums: List[int]) -> List[int]:
        convert=[2*nums[0]]
        max1=nums[0]
        for i in range(1,len(nums)):
            if nums[i]>max1:
                max1=nums[i]
            convert.append(convert[i-1]+nums[i]+max1)
        return convert
