class Solution:
    def groupAnagrams(self, strs: List[str]) -> List[List[str]]:
        ss=[]
        d={}
        for i in strs:
            t=''.join(sorted(i))
            if t not in d.keys():
                d[t]=[]
                d[t].append(i)
            else:
                d[t].append(i)
        for i in d:
            ss.append(d[i])
        print(ss)
anag        return ss
