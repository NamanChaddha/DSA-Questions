        def backtrack():
            if len(nums)==:
                result.append(copy of solution)
                return
            for choice in choices:
                if violates constraints:
                    continue

                make choice
                backtrack(update parameters)
                undo choice
